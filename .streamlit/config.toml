# Tema aplicado SEMPRE (local e na web). Garante texto escuro sobre fundo claro,
# inclusive na tabela editável de itens.
[theme]
base = "light"
primaryColor = "#C47A3A"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#e8eef8"
textColor = "#1B3065"
font = "sans serif"